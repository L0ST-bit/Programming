#pragma once
using namespace std;

int Header2(int k) {
	int i = 0;
	double  x;
	cout << "x   " << "sin(x)";
	for (double i = 0; i <= (3.14 / 4); i = (3.14 / 180)) {
		cout << i << "   ";
		for ()
	
	// ((a *= (i * i / (2 * i + 2) / (2 * i + 3))) > k) {

	cout << sh << ' ' << sinh(x);
	return 0;
}